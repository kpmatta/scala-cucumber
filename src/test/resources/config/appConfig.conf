s3Config {
    bucket = "bucketname",
    input.folder = "input/ket",
    output.folder = "output/key"
}

proxyConfig {
    host = "proxy.host.com",
    port = "8080"
}

dynamoDBConfig {
    region = "us-east-1",
    table.name = "tableName",
    dynamoDBThroughRead = "1"
    dynamoDBThroughReadPercentage = "1"
}